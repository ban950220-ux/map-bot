# 가까운 한 끼 — Codex 지침

## 먼저 지킬 것

- 작업 범위를 좁게 유지하고 기존 구조·기능·비공개 배포 설정을 보존한다.
- 비밀 파일과 실제 키 값을 읽거나 출력하지 않는다. 필요한 경우 환경변수 이름과 존재 여부만 다룬다.
- 아키텍처나 외부 서비스 흐름을 바꾸는 작업에서만 `PROJECT_CONTEXT.md`를 읽는다. 작은 UI 문구 수정에는 필요한 파일만 읽는다.
- `.openai/hosting.json`이 있으므로 제품 소스나 배포를 다룰 때 Sites 지침을 따른다. 문서·Codex 설정만 바꾸는 작업은 배포하지 않는다.

## 비용 우선 라우팅

- 기본은 주 에이전트 한 명이다. 하위 에이전트는 독립 작업이 실제로 병렬화되고 추가 토큰보다 이득이 클 때만 사용한다.
- 위치 찾기·의존 경로 확인은 `scout`; 명확한 구현은 `worker`; 위험 검토는 `reviewer`; 구조 변경이나 두 번 실패한 진단은 `architect`를 사용한다.
- 한 번에 하위 에이전트는 최대 2개만 사용한다. 같은 파일을 여러 에이전트가 동시에 수정하지 않는다.
- `gpt-6-astra` 승격은 `architect`로도 해결되지 않은 모호한 다단계 문제, 인증·데이터 손실·배포 범위 같은 중대한 결정, 또는 사용자의 명시 요청에만 허용한다.
- 탐색 결과는 관련 파일·심볼·현재 동작·의존성·남은 불확실성만 압축해 반환한다. 이미 확보한 내용을 다시 스캔하지 않는다.

## 코드 경계

- 서버 전용 비밀을 Client Component, 응답 본문, 로그에 넣지 않는다.
- ChatGPT 인증을 완화하거나 우회하지 않는다.
- NAVER Directions 5를 행렬 API로 오인하지 않는다. 현재 제한 병렬 처리와 abort/부분 실패 의미를 보존한다.
- 직선거리와 도로거리, 일반 소요시간과 교통 반영 시간을 구분한다.
- 공급자가 주지 않은 평점·리뷰·영업 상태를 생성하지 않는다.
- 새 의존성, D1/R2, 외부 API, 데이터 스키마는 요청에 필요할 때만 추가한다.

## 검증

- TypeScript/서버 변경: `npm run build`와 `npx tsc --noEmit`.
- 주변 검색 로직 변경: `node scripts/check-nearby.mjs`.
- 실제 API 검증, 비공개 사이트 배포, 접근 범위 변경은 사용자 요청 없이 실행하지 않는다.
